from tkinter import * # invoking the library
# * means all

window = Tk() # connecting window to the library

# Tk is the shortform of Tkinter

window.geometry('500x500+150+300')
# ('width*height+position of the window')
window.title('Hello Tkinter')
#providing the title of the window
window.mainloop() # allows to draw everything in the mainloop